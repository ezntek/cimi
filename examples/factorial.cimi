const factorial = fn(n: int)
    if n == 1 
        1
    else 
        factorial(n - 1) * n
    end
end

let max: int
echo("how many factorials")
read("max")

while i < max 
    let res = factorial(i)
    echo(i, "! is ", res)
    i += 1
end
