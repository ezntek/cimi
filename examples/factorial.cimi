fn factorial(n: int) 
    if n == 1 then
        return 1
    else
        return factorial(n-1) * n
    end
end

var max: int
echo "how many factorials"
read max

var i = 1
while i < max do
    var res = factorial(i)
    echo i, "! is ", res
    i += 1
end
